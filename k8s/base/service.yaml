apiVersion: v1
kind: Service
metadata:
  name: go-web-app
  namespace: go-web-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: go-web-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
